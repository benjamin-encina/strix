package com.strix.msadjuntos.repository;

import com.strix.msadjuntos.model.ComentarioRef;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComentarioRefRepository extends JpaRepository<ComentarioRef, Long> {
}
